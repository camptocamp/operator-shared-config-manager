apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operator-shared-config-manager.fullname" . }}-test-connection"
  labels:
    {{- include "operator-shared-config-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operator-shared-config-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
