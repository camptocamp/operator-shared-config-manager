[tool.poetry]
name = 'operator-shared-config-manager'
version = '0.0.0'
description = 'Not used'
authors = []

[tool.poetry.dependencies]
python = ">=3.10,<3.11"
kubernetes = "25.3.0"
kopf = "1.36.0"
pyyaml = "6.0"

[tool.poetry.dev-dependencies]
prospector = { version = "1.8.4", extras = ["with_bandit", "with_mypy"] }
types-pyyaml = "6.0.12.8"
