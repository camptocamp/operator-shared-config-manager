# yaml-language-server: $schema=https://raw.githubusercontent.com/camptocamp/c2cciutils/master/c2cciutils/schema.json

checks:
  required_workflows:
    codeql.yaml: False

publish:
  pypi:
    packages: []
  docker:
    images:
      - name: camptocamp/sharedconfigmanager-operator
